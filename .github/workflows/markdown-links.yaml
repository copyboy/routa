name: 'Markdown Links'

on:
  pull_request:
    paths:
      - '**/*.md'
      - 'tools/hook-runtime/src/check-markdown-links.ts'
      - '.github/workflows/markdown-links.yaml'
  push:
    branches: [main]
    paths:
      - '**/*.md'
      - 'tools/hook-runtime/src/check-markdown-links.ts'
      - '.github/workflows/markdown-links.yaml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  markdown-external-links:
    name: 'Check Markdown External Links'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check markdown external links
        run: node --import tsx tools/hook-runtime/src/check-markdown-links.ts
